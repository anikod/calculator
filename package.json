{
  "name": "calculator",
  "version": "1.0.0",
  "description": "just a simple calculator",
  "main": "index.html",
  "scripts": {
    "dev": "sass --watch --no-source-map style.scss style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anikod",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.62.0"
  }
}
